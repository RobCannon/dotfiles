#!/usr/bin/env bash
if [[ -f 'Pulumi.yaml' ]]
then
    stack_name=$(yq e '.name' Pulumi.yaml)
    workspace=$(find ~/.pulumi/workspaces -type f -name "${stack_name}*.json")
    jq -r .stack $workspace
fi